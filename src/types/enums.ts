export enum SexFilter {
  All = 'all',
  Male = 'm',
  Female = 'f',
}

export enum SortField {
  Name = 'name',
  Sex = 'sex',
  Born = 'born',
  Died = 'died',
}

export enum SortOrder {
  Asc = 'asc',
  Desc = 'desc',
}
